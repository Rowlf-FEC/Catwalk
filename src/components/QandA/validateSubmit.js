import React, { useState } from 'react';
import axios from 'axios';

function ValidateSubmit(props) {

  // this is just a helper function for validating the forms inside submit question and submit answer

  // If there is an invalid entry submission should be prevented and warning message given

  // Invalid entries are:
    // 1. Any mandatory fields blank
    // 2. Email provided is not correct email format

}

export default ValidateSubmit;